try {
node {
    def app

    stage('Clone Repository')
    {
        checkout scm
    }
	       
    }
		stage('apply changes to the environment'){
		
		sh "ls -l"
		sh "php -S localhost:5000"
		}
		
	}
		
} 